/** The comparison viewports of docs/UI/REFERENCE.md — the same ones the prototype was captured at. */
export const VIEWPORTS = {
  desktop: { width: 1440, height: 900 },
  mobile: { width: 390, height: 844 },
} as const;
