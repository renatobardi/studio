.env
.env.*
!.env.example
node_modules/

# macOS Finder metadata — never anyone's code
.DS_Store
scripts/ops/

.claude/

# Python (api/)
.venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/

# Coverage reports, produced by CI for the Sonar scan (#76) — never committed.
api/coverage/
# pytest --cov writes this next to the package it measured, not into the
# directory above; uncommitted either way.
api/.coverage
web/coverage/
